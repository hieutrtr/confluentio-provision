- name : Gather facts zookeeper
  hosts: zookeeper
  sudo: yes
  gather_facts: yes
  remote_user: ubuntu
  tasks:
    set_up:
      
- name : Start Confluent Kafka Server
  hosts: kafka-server
  sudo: yes
  gather_facts: yes
  vars:
    action: 'install'
  remote_user: ubuntu
  roles:
    - common
    - kafka-server
